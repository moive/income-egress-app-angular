import { SortIncomeEgressPipe } from './sort-income-egress.pipe';

describe('SortIncomeEgressPipe', () => {
  it('create an instance', () => {
    const pipe = new SortIncomeEgressPipe();
    expect(pipe).toBeTruthy();
  });
});
